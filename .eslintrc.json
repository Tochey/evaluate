{
    "extends": ["next", "next/core-web-vitals", "eslint:recommended"],

    "globals": {
        // "React": "true",
        "JSX" : true
    },
    "rules": {
        "no-unused-vars": [
            1,
            { "args": "after-used", "argsIgnorePattern": "^_" }
        ]
    }
}
